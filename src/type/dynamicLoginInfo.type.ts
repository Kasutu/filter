import { Time } from '../interface/dateAndTime.interface';
import User from '../interface/user.interface';

type DynamicLoginInfo = {
  [key: string]: any;
};

export default DynamicLoginInfo;

// const myArray: DynamicLoginInfo[] = [];

// let someValueArray: Time = {
//   hours: 0,
//   minutes: 0,
//   suffix: 'am',
// };
// var key = 'login';

// myArray.push({ [key]: someValueArray });
